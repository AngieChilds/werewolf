<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @village.name %>
</p>
<table class="horizontal_table" style="width: 100%; height: 10px;" border="1">
<tbody>

  <tr>
    <th width="40%">Player </th>
    <th width="10">user#</th>
    <th width="15%">alive</th>
<% if current_user.admin? %>
    <th width="15%">werewolf</th>
    <th width="10%">against</th>
   <th width="10%">voted</th>
   <% end %>
  </tr>
</tbody>
</table>

<%= render @village.games %>

<b>Add new player  </b>

<%= render 'games/form' %>

<%= link_to 'Edit', edit_village_path(@village) %> |
<%= link_to 'Back', villages_path %>
